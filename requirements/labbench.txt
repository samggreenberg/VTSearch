# VTSearch: LabBench dependencies (image search with SigLIP only)
#
# Pared-down requirements for the LabBench deployment: browsing/voting
# on an image dataset with the SigLIP embedder. Skips audio, video,
# document, text, and extractor deps to keep the image small.
#
# Used by docker/Dockerfile.labbench.

# ── Core framework ───────────────────────────────────────────────────
flask
flask-smorest
marshmallow
pydantic>=2
werkzeug
gunicorn
numpy
requests
tqdm
scikit-learn
transformers
pandas

# ── Eval / visualization ────────────────────────────────────────────
matplotlib
scipy

# ── VTSBrowse projection ─────────────────────────────────────────────
# The Browse canvas fits UMAP on the embedding matrix (vtscore.gpu_backends
# .umap_fit_transform, imported lazily). Needed by every deployment
# regardless of media type: nothing else pulls umap-learn in transitively,
# so omitting it leaves Browse broken at runtime rather than at build time.
umap-learn

# ── PyTorch (CPU) ────────────────────────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# ── Image media type ─────────────────────────────────────────────────
Pillow

# ── SigLIP embedder ──────────────────────────────────────────────────
sentencepiece
protobuf
