# `docs/experiments/` — index

One directory per study: a `REPORT.md` written on top of numbers generated
deterministically from the run's own CSVs, plus the generated tables and figures.
These are the **record of what a run produced** — they are archives, not plans,
and they are not pruned when the work they justified ships.

**Every study directory is named `YYYY-MM-DD-<slug>`**, dated when its report
first landed, so a bare `ls` sorts the archive chronologically and the newest
work is the last thing printed. The date is the *study's*, not its last edit: a
report that is later corrected, re-skinned or extended keeps the name it was
filed under, because that name is what every link in the tree points at. The
index below runs the other way — **newest first**. `scripts/check-docs.py`
enforces both halves: the name shape, and that every study has a row here.

**What a report owes its reader** (the long form is in the `grid-experiments`
skill): two significant digits unless a decision needs a third, arm differences
**paired** and carrying a standard error; **figures** emitted by a committed
script from the same CSVs as the tables; and **literal examples** of the errors
behind every error rate, so a reader can tell a model error from an annotation
error. Analysis scripts must be committed: the doc gate checks that paths cited
here resolve.

**Every study that simulates a user clicking owes the quality-over-clicks pair**
— how good that user's detector is as they click more, **averaged** (one panel
per dataset, one line per arm) and again **per run** (one panel per arm, every
seed as its own line). Click 0 is the free text sort, so the far left is what
typing was worth and the far right is what clicking was worth. One
implementation, [`scripts/experiments/calibration/curves.py`](../../scripts/experiments/calibration/curves.py);
do not write it again.

**…and it owes an interactive `viewer.html`, linked from the report.** The PNGs
answer the questions the analyzer asked; the viewer is what lets a reader ask
their own — any dataset (one / all / each), any category (one / all / each), any
subset of arms, any subset of embedders, seeds averaged or every seed, and any
metric the run emitted (cost, precision, recall, F1, FPR, FNR, average
precision, AUROC). Two draw toggles decide the shape: **overlay** puts every
varying dimension on one chart in distinct hues (off, each gets its own chart
with the ±1 SD shadow, which is the only place that shadow is readable), and
**oracle threshold** adds the cut the test labels say the model should have
used, dotted beside the solid line it achieved. Two more reference marks are
notched into the margins: the free text sort at the left and, for a run
launched with `CALIB_SKYLINE_ARMS`, the supervised skyline at the right. Built
by [`scripts/experiments/calibration/viewer.py`](../../scripts/experiments/calibration/viewer.py)
from the same CSVs as everything else, self-contained, no network.

**A page can be re-skinned without its results.** The template carries all of
the viewer's behaviour and the payload only the study's numbers, so
`python viewer.py --reskin docs/experiments/*/viewer.html` pushes a template
improvement onto every committed report — even one whose results directory is
long gone. It cannot add data the payload never carried, so a *series* still
needs a rebuild from the cell CSVs. Both of the ones added in #3325 were
backfilled that way in #3326: the oracle cut cost nothing to recover (its inputs
are on every base row ever emitted), and the skyline was measured by a second,
cheaper pass over the same cells and merged in with `--skyline-results`, because
it is vote-independent and re-running the loop for it would have replaced the
performance rows the reports' tables were read off.

**A long study also gets a `report.html` reading copy, in the same directory.**
`REPORT.md` is the archive and renders inline on GitHub, but bitmap plots cannot
be zoomed and a 900-line document wants a table of contents. `report.html` is a
self-contained page — vector figures inlined, photographs embedded, one file,
opened in a browser (GitHub renders raw HTML as source). **Generate it, never
hand-write it:** `scripts/experiments/calibration/make_bench_html.py` builds it
from the study's own `REPORT.md`, so the narrative has one source and the two
cannot drift — edit the report, re-run the script, commit both. It lives beside
the markdown it was built from precisely so a reader never has to choose which
of two directories a study is in.

**A few early studies are narrative-only**, from before the CSV-and-figures
convention: their `report.html` *is* the record and the `REPORT.md` beside it is
a short pointer carrying the question and the verdict. Their rows below say so.

**The other archive.** [`docs/plans/`](../plans/) holds work still owed.

Add a row here whenever you add a study — at the top, since the table runs
newest first.

| Study | Question | Verdict |
|-------|----------|---------|
| [`2026-09-20-docmarks-contamination/`](2026-09-20-docmarks-contamination/REPORT.md) | How many of the pages DocMarks scores as negatives actually carry the mark? `roster.eligible_pages` splits every page into positive / known negative (a source exhaustively checked, so absence is verified) / **presumed negative** (contamination-safe but nobody looked), and censused across the v4.0 roster at tier `m` the presumed ones are **1,101,461 of 1,128,557 page-class pairs -- 97.6% of everything the benchmark scores**. `CONTAMINATES` removes the *systematic* risk by construction; the residual had never been measured, and an unlabelled positive does not make a benchmark noisy, it makes a correct retrieval score as a **false positive**. Two samples per class, reported separately: a detector-free `uniform` draw, and the `ranked` top-50 of a SigLIP ranking where AP damage would concentrate. Nothing automated may mint a negative -- a detector's misses sit exactly on the faint marks an advanced method should win on. | **Zero, to the limit of 439 human judgements.** 0 of 239 uniform (**95% upper bound <= 1.26%**) and 0 of 200 ranked, over four Tobacco800 classes plus a SPODS and a StaVer spot check. **9 of 9 planted positives found**, so the zeros are evidence someone looked rather than evidence someone clicked; **100% test-retest** against 159 pages judged in two earlier partial passes. A study may now say "AP against a pool carrying at most ~1.3% unlabelled positives" instead of implying zero, and the ranked arm is the stronger half: if contamination existed at a rate that could distort AP, the top of a similarity ranking over ~44,000 candidates is where it would surface. **Does not** license calling the corpus clean: six classes of 27, per-class n~50 (own bound ~6%), anchor pages not sampled, and nothing here touches the four UCSF classes whose source is excluded from their distractors entirely (#3922). |
| [`2026-09-22-docmarks-ucsf-contamination/`](2026-09-22-docmarks-ucsf-contamination/REPORT.md) | Can the four UCSF roster classes score against the rest of UCSF instead of ~2,900 pages? The letterhead pull banded exactly UCSF's Tobacco industry (13,857 pages), where the band classes miss most of their own mark (#3922); the other five industries were never looked at by anything, a provenance argument that needed checking. `contamination_sample.py --mode ucsf-unbanded`: 50 random pages per class weighted 3:1 toward Food (Kraft, Nabisco), plus each class's SigLIP top 30, plus planted controls. | **Zero.** 0 of 152 random Food (<= 2.0%), 0 of 48 random other (<= 6.3%), 0 of 120 ranked; **6 of 6 controls found**. **v4.1** narrows `CONTAMINATES["ucsf"]` to `ucsf:Tobacco`: pools ~2,900 -> ~46,700 at tier `m`, a minor bump, the four UCSF classes re-scored. The source shortcut goes (rank-UCSF-first control AP 0.20-0.97 -> 0.00) but a letterhead-style one stays (rank-UCSF-Tobacco-first AP 0.53-0.97), so a UCSF-class number is method-vs-method only until the banded pool supplies reviewed negatives. |
| [`2026-09-20-docmarks-v4/`](2026-09-20-docmarks-v4/REPORT.md) | What does DocMarks measure once the roster grows from 23 classes to 27 (#4050)? The UCSF member proposals, a second completeness pass and the query-crop alternates took the corpus to **2,004 instances**, and deduping 145 repeated page records took it to 199,855 pages -- a roster change and a page-set change, so v4.0 is a major bump and the baselines are re-run rather than re-scored. `eval_retrieval.py` on tiers `s` and `m` against the pruned and relabelled cells. | **The completeness passes removed a degenerate shortcut; the new classes are not yet scored on the same footing.** At v3.1 `source_prior` scored a perfect **1.00 mean AP** on the eligible pool -- ranking a class's own source answered the benchmark without looking at a pixel. It falls to **0.92**, and per class the five Tobacco800 classes that grew (`ajj10e00` 50->399, `aah97e00` 63->370, `aeq93a00` 14->130, `ald41a00` 51->206) are **exactly** the five whose prior fell below 1.000; the five that stayed flat kept it, as did all 11 SPODS and both StaVer classes. Real retrieval is unmoved: tier `s` own_verified siglip **0.12** both sides, naive 0.11 both sides, eligible 0.20 -> 0.18; `vlad` stays at 0.00-0.01 (#3911). **The four UCSF classes are a separate measurement**: `eligible_distractor_sources` excludes their own source, because no one has verified the other 197,077 UCSF pages lack the mark (#3921), so they are ranked against **~2,900** pages where every other class faces **45,000-49,600** -- a 16x difference that accounts for their high AP (`bat_leaf` 0.42 against `aah97e00` 0.02), and makes any 27-class mean an average over incompatible pools until #3922 lands. Three defects cleared first (#4052 a held vote applied as its opposite, #4054 145 duplicate page records with 13 disagreeing about the marks, and the cells that inherited them); the cell repair was **prune + relabel in ~3 minutes** rather than the ~15 h a rebuild would have spent recomputing identical vectors. Tier `l` and Stage 1 not re-measured. |
| [`2026-09-20-widen-c-4056/`](2026-09-20-widen-c-4056/REPORT.md) | Widen *C* from the shipped 25 to every class meeting the count requirement (#4056)? #3983 showed per-class supply is not the cap, so the open question was what widening does to the NEGATIVES, which are drawn as *holds none of C*. First a probe over each roster's clean set; then, once the set was actually rebuilt, the 75 pre-existing cells measured against the preserved 25-class build, reporting AP both as shipped and size-matched. Two columns. | **The roster is free and so, nearly, is the accuracy: rebuilding costs −0.03 AP and almost all of it is prevalence.** 53 classes ship (all 25 survive, 28 join; `wine glass` held out by owner ruling because SCALE_CLASS_MERGES folds it into `cup`), **159 cells, 0 short** of `SCALE_N_POS`, headroom 1.48x. Rebuilt and measured: **−0.030 +/- 0.004** (`siglip`) and **−0.028 +/- 0.004** (`siglip2_l`) as shipped, but **−0.003 / +0.001 size-matched** and dAUC **+0.002** — and AUC cannot see prevalence. A rebuilt cell just carries 23,891 negatives against 16,535. Rankings untouched. **A first reading of +0.24 AP was WRONG IN SIGN and is corrected in the report**, having been merged first (PR #4063): it varied the barren component alone, and the barren draw is capped at `SCALE_N_NEG` in BOTH builds while #3667's cross-class negatives more than double (6,635 → 13,991), so the emptiness never reaches a capped draw. The mechanism is nonetheless real — clean candidates 49,503 → 16,091, classes held per clean pool image **1.163 → 0.000** — and constrains any future change to how the pool is SIZED. Side findings: `coco_folds.py`, which measured the first 25 reviewer briefs, was deleted by the VG retirement and three live documents still name it (#4060); `dining table` is 10% `dining_table` against 34% `tablecloth`, `tv` is half computer monitors, `potted plant` three quarters cut flowers, `mouse` never an animal. The 84 new cells' own numbers, and whether per-class pools are immune by construction, are unmeasured. |
| [`2026-09-20-size-bands-4051/`](2026-09-20-size-bands-4051/PREREG.md) | Does a detector trained at one box-size band find the object at another (#4051)? #3156 answered the band question on the **diagonal** only -- `scale_core._evaluable` excludes an image holding the class at another size from the cell, so the other six squares of a 3x3 were not unmeasured but *unaddressable*. #4047 made them reachable; this fills the table on `coco_quarry`, 25 classes x 3 training bands x 3 test bands, two columns, 20 seeds. One threshold, one FPR, three FNRs -- the bands share a negative pool by construction, so there is no per-size FPR to report. | **Pre-registered, not yet run.** Predictions and the bars they are judged against are in the PREREG; the run needs the GRID. |
| [`2026-09-20-negative-pool-3986/`](2026-09-20-negative-pool-3986/REPORT.md) | Should `coco_quarry` draw negatives per class instead of from one shared pool (#3986)? The issue argues the shared pool both caps the class list and draws negatives 0% co-occurring where a representative draw is 86%, and warns that "every published cell is conditioned on" the answer. Three instruments, each deliberately the one #3667 used so the numbers compare rather than being two scales that both say "harder": a text-sort probe with `n_neg` pinned across arms, a trained linear head scored at a cut pinned to 5% FPR, and a mixture decomposition recovering the true 100%-vs-0% contrast from the shipped 40%-co-occurring baseline. Four single-vector columns, 25 classes, 75 cells. | **The representativeness cost is the minor one -- make the change for supply, not accuracy.** Switching to a representative pool moves a measured cell by **-0.002 AP** (largest dAP across 300 measured cells 0.007, and its sign is not consistent), and **0 of 75 cells change winning column**, so no published `coco_quarry` number needs re-reading. Not because the shortcut is absent: it is at full strength, **1.97-2.25x** co-occurring over barren in all four columns against #3667's **1.88** on `vg_scale`, with the same `@small` (2.67-3.00) to `@large` (1.33-1.53) gradient. #3667's fix already made **40%** of every cell's negatives the hard kind, so going the rest of the way to 61% costs nothing measurable; under the pre-#3667 design the same change would have moved AP by **-0.193**. **The issue's premise number is wrong for this dataset** -- its "~84% barren" is #3670's `vg_scale` figure, where `coco_quarry` is 16,535 negatives per cell at 60% barren. What survives untouched is the supply cap: "holds none of *C*" gives 1.5x headroom at 54 classes where per-class pools give 5-11x, and that is now the whole case for the change. Patch and region arms unmeasured (#4043). |
| [`2026-09-19-docmarks-detect-cap-3908/`](2026-09-19-docmarks-detect-cap-3908/REPORT.md) | Is the 2 MP `MAX_STRUCTURAL_DETECT_PIXELS` right on a retrieval task (#3908)? #3907 pinned it on a synthetic warp sweep that scored verification of true pairs with no distractors. Six caps (0.25 MP to native) x two keypoint budgets (1,024 and 8,192), exhaustive SIFT ranking over DocMarks v3.1 tier `s`, plus per-page detect time and peak RSS on a 150-page sample spanning 0.13-63 MP. | **Keep 2 MP -- it is the optimum, not a compromise.** At the 8,192 budget it ranks the roster at **AP 0.88** against 0.74 uncapped (**-0.139 +/- 0.028**, worse on 20 of 23 classes) and 0.76 at 4 MP; 1 MP ties it (-0.003 +/- 0.032) at half the storage, so a tier-`l` cell should use 1 MP (78 GB against 167 GB). **The cap and the keypoint budget are coupled**: drop the budget 4x and the best cap moves 4x, so the SHIPPED pairing (`DEFAULT_MAX_FEATURES` 1,024 with 2 MP) scores **0.16** where 0.5 MP scores **0.69** (+0.532 +/- 0.074). Above the optimum the cap picks *which* keypoints (at 1,024 the median page keeps exactly 1,024 at every cap, and AP still runs 0.08-0.69); below it, supply runs out and both budgets collapse together at 0.25 MP. Uncapped is also a memory hazard: **14.4 GB peak RSS against 0.77 GB**, and 11.3 s on a 63 MP page against 0.39 s. OpenLogo not run, so the per-media-profile question stays open. |
| [`2026-09-18-vg-diversity-3997/`](2026-09-18-vg-diversity-3997/REPORT.md) | Does Visual Genome's non-COCO half carry anything COCO cannot (#3997)? `anchor_to_coco` claims dropping it loses "VG's non-COCO diversity for nothing", and the `coco_quarry` plan makes settling that the item that runs first, as the only live argument for keeping VG. Two halves: a linear provenance probe on the embeddings, matched within `class@band`; and the shipped head (`fit_linear_svm_head`) ranking held-out positives against a held-out half of the shared pool, per cell, with #3926's hand-verified subset as a control. | **Differently distributed, but not harder -- the difficulty argument does not survive.** The probe's own flag was wrong: it split on `labels_exhaustive` ("COCO answered OR a human reviewed one class"), putting 2,138 reviewed-but-off-COCO positives on COCO's side, 41% of all 5,237. On `coco_scored`, positives are **55% off-COCO**; matching composition away takes provenance AUC from 0.57 to **0.53-0.54** across five embedders, so about half the readable signal was class and band. On hardness every statistic leans the **other** way: off-COCO ranks slightly better, **+0.11 to +0.16** of log1p(rank) on three embedders, 34-37 of 58 cells agreeing in sign. The obvious statistic hides this -- percentile-of-pool saturates at 0.85-1.00 and reads as a flat nothing -- so per-image ranks are stored and `--aggregate` re-reads them without refitting. The verified-only control is twice the effect and is **not** the clean number: #3926 verified by asking whether a human could see the object, which selects for clear instances, so it bounds the label-noise share rather than revealing true difficulty. #3670/#3702's composition argument cites the old figure and now rests on a re-derived one. |
| [`2026-09-18-coco-only-supply-3983/`](2026-09-18-coco-only-supply-3983/REPORT.md) | Does `vg_scale` need Visual Genome at all (#3983)? `exact_supply.py` reported 20 of 36 cells short of 300 COCO-anchored positives, but it draws candidates from `vg_source()`, so its "COCO half" is the VG-COCO overlap (51,497 images) rather than COCO 2017 train+val (123,287). Re-run with the pool unbounded by VG, importing the shipped `band_for` rather than restating it. | **COCO alone supplies every cell, with margin: 0 of 75 short of `SCALE_N_POS`=100**, thinnest `bus@small` at 177 (1.8x), and 49,503 shared-pool candidates against 9,900 needed (5.0x). Only 5 of 75 fall under 300, all `small` band. The two readings agree where they overlap -- `dog@small` 114 to 263 and `spoon@large` 106 to 334, against a 2.4x pool ratio. **The class axis roughly doubles**: 54 of COCO's 80 classes clear 100 in all three bands, 29 beyond the current 25. #3603's structural claim is independently confirmed (`giraffe` has 3 small-band images in COCO against 14 in VG -- a class that owns its scene fills the frame, whatever the source), but every class VG blocked for *vocabulary* reasons returns: `potted plant` 1 to 299, `traffic light` 58 to 745, and `motorcycle`/`surfboard`/`skateboard`/`snowboard` stop being aliases of `bike` and `board`. Not measured: whether the result is as *hard* -- supply is not difficulty. |
| [`2026-09-17-vg-scale-corrections/`](2026-09-17-vg-scale-corrections/REPORT.md) | A running record of what `vg_scale` corrects in Visual Genome, kept so the corrections can be described later: each kind of defect, how often it occurs, and a literal example of it. Not a study -- every number is copied from the work that produced it, and new kinds are appended as review finds them. | **4,709 correction rows against VG's annotation so far.** The kinds: a class present and unnamed (3,837 images added as boxed positives, #3940); a box on a smaller instance than the frame's main one (**8.3%** of stored-box positives -- one band too small, and 55 of 102 triage redraws left the small band); a box on the wrong object (**1.2%**); a box on no object at all (`bicycle` 2388483 sits on empty road beside the ridden bicycle, traced end to end to rule out a coordinate fault); one object annotated many times (21 bench boxes on 2320173, 23 bird boxes on 2335647 -- harmless to the band, fatal to review); and the definitional cases where VG saw the pixels correctly and the class does not admit the object (kettle/`bottle`, spreader/`knife`, car seat/`chair`, planter/`vase`). |
| [`2026-09-17-docmarks-ucsf-classes-3921/`](2026-09-17-docmarks-ucsf-classes-3921/REPORT.md) | Can #3902's one-mark UCSF band classes become roster proposals (#3921, #3922)? Each gets a query crop, a SIFT member search over all 47,222 UCSF tier `s`/`m` bands plus Tobacco800, and crop-to-crop checks against the Tobacco800 roster crops. Every hit page goes on one-screen review sheets (1,641 cells) with first-pass suggestions. An apply path that admits only reviewed UCSF pages to a class's pools is built and tested but not run. | **9 proposals: 5 extend a Tobacco800 class and 4 are new, after the owner dropped `bw_1990s_leaf`**; they await owner rulings. Crop-to-crop inliers settle each relation: 14–160 for the extensions, at most 6 for the new marks. The band classes miss much of their own mark: the 20-band PM crest class misses 289 crest pages in `s`+`m`, and RJR block misses 87. Every auto crop was re-cut by hand (10 of 10, flag caught 2), because typeset text in a crop matches any page's text (4,700 vs 380 pages at 8+ inliers). Collapsed RANSAC fits needed a box-size gate. Hits with 20+ inliers carried the mark on 470 of 472 cells; hits with 8–11 inliers carried it on 50 of 351. |
| [`2026-09-17-gp-head-3954/`](2026-09-17-gp-head-3954/REPORT.md) | Should the head be a Gaussian process conditioned on the votes (#3954)? A CPU pilot: `gp_rbf` / `gp_dot` trainers, two posterior-driven Hard picks and a rank-transferred cut built into the harness, then measured on `caltech101_m` (SigLIP, 6 categories, 5 seeds, 150 clicks) against the shipped detector and its plain cross-calibrated twin. | **The GP's probabilities are better calibrated (Brier 0.036 vs 0.091, ECE 0.18 vs 0.30 at 128 labels, paired, tens of SEs) and it changes nothing a user would see.** Ranking is perfect for every head (AP 1.00), so every Stage B difference is the threshold: the raw cross-calibration cut collapses on the GP's shifting probability scale (flags the whole haystack on 8.5% of steps), carrying it by rank under-admits half the positives, and on the app's fold-less blend no GP arm beats the plain cross-calibrated linear SVM (best GP arm +0.013 +/- 0.004 AULC cost; `app_xcal` 0.006). Max-variance is the only GP acquisition rule that keeps up, and only for 50 clicks; the straddle is no better than the rank pick. Side finding: the shipped fused arm is worse than its own cross-calibrated twin on this 838-image set (+0.052 +/- 0.014 AULC), the binary-voting fusion regression already on record. GRID study with a GP-native cut: #3959. |
| [`2026-09-18-docmarks-stage1-3928/`](2026-09-18-docmarks-stage1-3928/REPORT.md) | Can DocMarks' tiled-VLAD Stage 1 be **stored** rather than recomputed per run (#3928), and how far can the stored tile be compressed? A cell per width (raw 8,192, 512, 256, 128 dims, whitened PCA sliced from one fit), on tiers `s` and `m`: Stage-1 recall@K, AP once #3911's saved SIFT inliers re-order the top K, and wall-clock per query. | **The stage is affordable and the compression is not a loss, but the width depends on the pool.** 256 dims stores **23 KiB/page against 818 KiB raw (35x)**; at tier `s` it ranks **above** the raw tile at every K (0.84 vs 0.77 at K=1,000) and **matches exhaustive at K=2,000** (0.879 vs 0.876). At tier `m` that parity **does not hold** (0.77 vs 0.83) and the widths separate, so **512 is the width to build** (0.73 vs 0.70 at K=1,000; 0.38 vs 0.30 alone). Search is **1.8 s/query at 50,000 pages** against ~100 s exhaustive, and tier `l` is a **4.4-8.8 GiB** cell instead of 156 GiB, ~4.5 h to build. StaVer gains from shortlisting (`stampds-00213` 0.12 exhaustive -> 0.73); `cgr96c00` (117-keypoint crop) is 0.00 and the two-crest `aah97e00` falls to 0.26, quantifying the need for #3949. **Build lesson: 40 pool workers x 40 BLAS threads ran at 1.05 pages/s; pinning `OMP_NUM_THREADS=1` gave 11.3 (9.7x).** v3.1 corpus, not comparable to the probe's row above. |
| [`2026-09-17-docmarks-tiled-vlad-3928/`](2026-09-17-docmarks-tiled-vlad-3928/REPORT.md) | Can a Stage 1 narrow DocMarks pages down before SIFT verifies them (#3928)? #3911 found page-level VLAD at chance and SigLIP shortlists losing most of exhaustive SIFT's AP at tier `m`. Probe: VLAD per overlapping page tile, max over tiles, as a shortlist re-ordered by #3911's saved SIFT inliers, on tiers `s` and `m`. | **Tiled VLAD is the right shape.** Alone it ranks at **0.40 / 0.25** (tier `s` / `m`; page VLAD 0.029 / 0.003, SigLIP 0.12 / 0.076). As a top-1,000 shortlist SIFT verifies, it keeps **0.71 / 0.59 -- 91% / 78% of exhaustive** at K = 20% / 2% of the tier, **+0.30 +/- 0.07 over a SigLIP shortlist at tier `m`** (18 up, 3 down). SPODS logos 0.89 of 0.98; Tobacco800 (0.47 of 0.75) and round stamps lose most, and three classes get nothing from Stage 1. Tile vectors cost ~0.9 MB/page uncompressed. |
| [`2026-09-17-docmarks-structural-3911/`](2026-09-17-docmarks-structural-3911/REPORT.md) | Why is structural search at chance on DocMarks (#3904), and what fixes it (#3911)? SuperPoint+LightGlue (the 2026-07-13 winner) against the shipped SIFT at larger page budgets, each verifying every page of tier `s` for the 23 roster classes, then tier `m`; VLAD and SigLIP as Stage 1 replayed from the saved inliers. | **The fix is the budget, not the backend.** SIFT keeping up to 8,192 keypoints/page (median 5,922 under the 2 MP cap) ranks at **AP 0.78** on tier `s` and **0.75** on `m` -- **+0.67 +/- 0.06 over SigLIP, better on all 23 classes**, and +0.39 +/- 0.07 over SP+LG (0.39). 4,096 gives 0.57, 16,384 adds nothing. Prerequisite: the 0.1 scale floor (#3912). **Stage 1 is now the bottleneck**: VLAD from the same features is at chance (0.029 / 0.003), a SigLIP top-1,000 shortlist keeps 0.51 on `s` but 0.28 on `m`, and exhaustive verification is ~100 s/query at 50k pages (#3928); a tier-`l` cell at 8,192 is ~170 GB. Checking the top hits found Tobacco800 roster classes missing members in off-roster classes (#3927). |
| [`2026-09-16-docmarks-bands-3902/`](2026-09-16-docmarks-bands-3902/REPORT.md) | Can SigLIP turn UCSF letterhead bands into classes where `phash` could not (#3902)? All 14,002 `letterhead_author` bands embedded, single linkage swept per author, and every proposed class of >= 20 bands called by eye from 24 random members. | **Yes, as audit candidates rather than classes.** At cosine distance 0.10, **11 classes are each one printed mark** -- 10 distinct marks over ~2,300 bands, including the Lorillard script (1,213 bands, 24/24) and a 1960s *P. Lorillard Company* crest #3901 never sampled -- where `phash` produced none. The same partition holds 6 junk classes of blank or typed bands (~1,600 bands; blank clusters well), 2 typeset layouts, and one mixed class: the 1,357-band Philip Morris class is crest on 13 of 24 and typeset heading on 11. Three defects no threshold fixes: junk needs a mark-presence gate; the BAT leaf is split across two author queries; and recall is poor on memo layouts -- the RJR block logo is on ~550 bands but only 20 cluster, so a band class would leave most of its own positives in the distractor pool (#3921, #3922). |
| [`2026-09-17-docmarks-crops-3912/`](2026-09-17-docmarks-crops-3912/REPORT.md) | Why would three Tobacco800 query crops not match the page they were cut from (#3912)? A crop is that page's own pixels, so #3904's 0 inliers at every keypoint budget could not be an ordinary matching failure. Crop vs raw re-cut vs decode for 8 classes, then every roster class's crop against its own page, 8 positives and 8 same-source negatives, judged at four scale floors. | **The matcher's scale floor, not the crops.** Crops are pixel-identical to their boxes at decode scale 1.0. `SiftMatcher` fits in normalised coordinates, so a crop fits its own page at about crop width / page width (158 / 2,544 = 0.062), and the `scale < 0.1` floor threw every such fit away, including 38 inliers for `bqz95d00` on the stored features. **Floor lowered to 0.03**: own pages verify 16 -> 23 of 23, positives with 8+ inliers 111 -> 166 of 182, negatives 5 -> 13 of 184; every Tobacco800 class now verifies its positives. The floor still removes collapsed fits at scale ~0 (up to 151 inliers on unrelated pages). No corpus change; the stored cells stay starved at 1,024 keypoints (#3911). |
| [`2026-09-17-docmarks-food-3914/`](2026-09-17-docmarks-food-3914/REPORT.md) | Do Tobacco800 roster marks appear, unlabelled, on UCSF's non-Tobacco pages, which the per-page contamination rule (#3904) still scores as negatives? Philip Morris owned Kraft and RJR owned Nabisco. SigLIP's top 8 non-Tobacco UCSF pages for each of the 10 Tobacco800 classes on tier `m`, plus every non-Tobacco page whose metadata names a company, looked at by eye. | **No leak; `CONTAMINATES` unchanged.** **0 of 80** top-ranked pages and **0 of 49** company-named pages carry a roster mark; Kraft and General Foods pages carry their own marks, never the Philip Morris crest, and most "Reynolds" matches are people. What SigLIP ranks highest instead is its confusers: 32 blank bordered photocopies, 21 clippings and 12 standalone logo images, **71 of 80 from Food** though Food is 36% of the pool -- older paper scans whose texture SigLIP ranks near a scanned mark. Not exhaustive over the 15,796 Food pages, and tier `l` unchecked. |
| [`2026-09-16-docmarks-letterhead-3901/`](2026-09-16-docmarks-letterhead-3901/REPORT.md) | Do UCSF letterhead bands carry a mark at all (#3901)? The README's `letterhead` pass had never run, and v2 could not say whether `phash` failed because the marks are absent or because it cannot see them. 40 bands per candidate author, 320 in all, called by eye on numbered sheets. | **The marks are there: 157 of 320 bands (49%) carry a printed mark**, 98 (31%) a typeset heading only, 64 (20%) none. Within an author the mark repeats -- Lorillard's script wordmark on **37 of 40**, the Philip Morris crest on 26, the old B&W leaf monogram on 21 -- so `phash` failed on present marks by hashing band layout, and the SigLIP band descriptor (#3902) is worth running. The Council for Tobacco Research and the Tobacco Institute are typeset-only (0 of 40 each), a `distinctive` decision rather than classes; the Solr author query leaks (`AMERICAN TOBACCO` returns British American Tobacco; a Lorillard band carries the Philip Morris crest); and the fixed 22% band cuts some marks. One reviewer, every call recorded against its sheet. |
| [`2026-09-16-docmarks-eval-3904/`](2026-09-16-docmarks-eval-3904/REPORT.md) | The first scoring of the DocMarks roster (#3904): 23 classes searched with their query crops over tiers `s` and `m` by SigLIP, VLAD and VLAD plus the app's top-50 re-rank, through the contamination rule -- with a source-only control, three candidate pools, and a crop-vs-page inlier diagnostic. | **None of the four findings is the number it was built for.** (1) **The contamination rule was inert at its own grain**: `classes.json` records `ucsf`, not `ucsf:Tobacco`, so all 3,182 UCSF Tobacco pages in tier `m` were scored as negatives for the Tobacco800 classes -- and SigLIP's top-ranked ones carry the class mark unlabelled (Lorillard at UCSF ranks 1-4); fixed per page, Tobacco800 SigLIP AP on `m` goes 0.015 -> 0.083. (2) **The rule as written is a shortcut**: excluding a class's own source lets a ranker that ignores the mark score **AP 1.00**; counting it as known negatives drops the control to 0.029 (#3913). (3) **Structural search is at chance** (VLAD AP 0.006 / 0.001) because pages keep **1,024 keypoints**, mostly text: 0 inliers on positives with stored features, while all 5 SPODS logos separate widely at 16,384 (#3911); three Tobacco800 crops match not even their own page (#3912). (4) **SigLIP is the only ranker above chance**, AP **0.12** (`s`) and **0.076** (`m`). |
| [`2026-09-13-anchored-em-stop-3825/`](2026-09-13-anchored-em-stop-3825/REPORT.md) | Where should the **anchored** refit stop (#3825)? #3585 made the initialiser 5x cheaper and, measuring the loop next door, found `_anchored_em` had become ~90% of a calibration fold's fit -- running a parameter-delta criterion at 1e-8 that on real folds it never met. The proposed fix was #3585's own: stop on the log-likelihood at sklearn's 1e-3. Gated on the same 84-cell corpus, re-used without re-running a thing, plus four trajectory grids. | **Shipped at 1e-8, and the proposed 1e-3 is a REGRESSION** (Delta cost **+0.026 +/- 0.006** over 114 paired cells): at that tolerance the refit halts after 10 iterations with the components still bunched, before the minority one has migrated to the high mode -- the likelihood is nearly flat *while the loop is still doing the work*, which is the opposite of the flat ridge #3585 found. **The criterion ports, the number does not.** The incumbent is worse than the issue's 5-fold sample showed: median **113** iterations, **26.5%** of 4,493 refits exiting on `max_iter`, and `iter400` shows a 1e-8 parameter delta is *unreachable* on 10.7% even at twice the budget. A second axis the issue did not name decided the design: the free sample's likelihood, which the sort path watches, **falls on 36.6% of iterations** under an anchored M-step, so the shipped rule watches the weighted objective the loop actually ascends (identical without anchors, so `fit_score_gmm` is untouched bit for bit). At 1e-8: **1.9x** cheaper, admitted set moves by a **median of zero** and a p90 of one media in two thousand, non-convergence **26.5% -> 7.4%**. The controls carry the reading -- `param1e-6` moves 1.5% of cases, and merely letting the incumbent *finish* moves the line 146x further at the tail than this change does. Uncomfortably, **no early-stopping arm beat the incumbent's objective once in 4,493 folds**; the A/B's floor is ~+/-0.004 of cost (#3840). Follow-ups #3839, #3840. |
| [`2026-09-13-folded-supply-3818/`](2026-09-13-folded-supply-3818/REPORT.md) | Of the 441 confirmed positives #3696 found that VG DID name and the pile does not designate (#3818), how many are supply the build is leaving on the table and how many are ordinary over-subscription? Read off the loader's own passes -- `read_vg_labels` through `designate_cells` -- by asking after each one whether the class is still on the image, so the first pass to drop a pair is the answer rather than a guess. | **Over-subscription, 80% of it: 355 of 441 reached their cell's supply and lost a seat to a hash rank**, in cells holding 20,118 candidates for 2,700 seats (1.4x at `bus@small` to 22x at `bus@large`). The other 86 are the band rules working -- 77 scatter (median **3.3x** inflation against a 1.5x cut; only 2 of the 77 would band differently if the cut moved) and 9 oversize (**80.2%-98.5%** of the frame). **The control is the finding's backbone**: divided by every queue image holding the class, scatter reads a 3.6x excess that is entirely the conditioning -- the 441 are undesignated by construction and a scattered image can never be designated -- and conditioned the same way, the 441 split 80/17/2.0 against the population's 82/15/2.6, class for class. So the confirmed errors are an unremarkable sample of supply the build has and does not need; **no membership has to move before the next rebuild**, and no repair issue is filed. All seven pre-band outcomes and the designation alarm came back **0**. Conditioning: 9 of 25 classes (#3814, #3819). Follow-up #3822. |
| [`2026-09-13-vg-silence-3696/`](2026-09-13-vg-silence-3696/REPORT.md) | How often is VG silent about a class that is really there (#3696)? The rate that justified #3670's negative-pool composition, which the composition then made unmeasurable by drawing all 9,900 shared negatives from the COCO-scored half. Read off the exhaustive pass's own answers instead, at no extra labour: every class VG did NOT name in a queue image is a measurement of its silence against a human reference. 81,363 silent pairs over 3,397 images; 9 of 25 classes finished. | **1.0%** [0.90%, 1.1%], **bound 1.7%** -- so at most **104** of `vg_scale_deep`'s 6,264 unprovable negatives are contaminated (#3723), landing near the middle of #3666's independently measured **1.40%** [0.68, 2.86] pool error rather than at its very top, where the uncorrected reading sat. **The correction is the finding: 524 of 815 confirmed errors (64%) are images VG DID name** -- 441 under a spelling the build folds but never designated (#3818), 83 under one `lift_ambiguous` refuses on purpose -- and neither can reach a negative pool. Reading designations as silence gives 2.8%, a valid bound that is loose by 2x, and it *re-ranks* the classes: `bus` falls 2.7% -> 0.51%, `bench` holds at 2.5%. Conditioning: an upper bound on the UNIFORM rate, since the queue is clutter-selected (#3667, #3679); 0.53 pts of the bound is screening mass nobody was shown (#3768, 0 of 600); `dog`, `fork` and `vase` are excluded for having been voted under a rule that has since moved (#3814). |
| [`2026-09-13-gmm-init-3585/`](2026-09-13-gmm-init-3585/REPORT.md) | Replace `fit_score_gmm`'s sklearn `GaussianMixture` with our own EM (#3585), and establish that the replacement is safe. The fit is the largest single term in a cosine/text sort and the larger half of a calibration fold, and swapping it **moves the fit**, so the deliverable is a measurement: 84 cells of real `fit_fold_anchored_cut` inputs plus 62 real cosine sorts, every candidate replayed through the whole production chain, then a two-grid trajectory A/B. | **Shipped.** The new fit changes the admitted set on **7.0%** of 2,258 fold cases by a median of **2 medias**; keeping sklearn and changing only `init_params="k-means++"` changes **16.1%** by a median of **11.5**, so the rewrite is more faithful to the incumbent than the smallest perturbation of the incumbent is. Its large moves are localised (26 of 34 at click <= 10, 26 of 34 in one 400-media environment) where the control's are spread over all six. **What made it faithful was the stopping rule, not the init**: the first cut reused the anchored loop's parameter-delta rule and was 2.8x *slower* than the call it replaced while moving up to 35% of a sort; stopping where sklearn stops (log-likelihood improvement < 1e-3) is the whole difference. At that tolerance neither fit is better - 2,369 wins to 2,146, median **+5e-7 nats** - and the arms that *do* fit better are the ones that move the cut most. Cost: **7.5x** per fit at 2.5k scores, **12.3x** at 50k, and **1.7x** on a whole sort - which also corrects the issue's own "91-95% of a sort" to **48.5%**, measured against the function the route calls. 114 paired A/B cells resolve nothing (cost -0.0054 +/- 0.0050). Handed over three follow-ups, the largest being that the anchored EM now *is* a fold's cost and **exits on `max_iter`**. |
| [`2026-09-11-calibration-seed-3796/`](2026-09-11-calibration-seed-3796/REPORT.md) | How wide is the distribution the pinned Train/Calibrate split draws from (#3796)? Production pins the fold split to `CALIBRATION_SPLIT_SEED` and #2934 pinned it on purpose, so every eval number here is one sample from a spread nobody had measured. The grid is inverted: the **cell seed is held** so the data cannot move, and the split is redrawn 20 times. 1,000 cells, 5 classes x 5 cell seeds x 20 draws x three geometries, 150 votes on `vg_scale_any`. Executes [`PLAN.md`](2026-09-11-calibration-seed-3796/PLAN.md), pre-registered before the array. | **The split is the larger half of the noise every study here already lives with** — **70%** of the cell-to-cell variance a study bootstraps over is the calibration draw, the one axis no study varies, against **30%** for the cell seed, the axis every study does. Median sd of cost across draws **0.022-0.060**, and **75-83%** of draw pairs inside one block differ by more than the **0.013** #3287 reported as its headline. **Most of it is not the cut**: the ranking term carries 0.39-1.15 of the variance against 0.13-0.73 for the cut, `threshold` sd is only 0.011-0.019, and **75 of 75 blocks are voting on different media by click 8** — reseeding a calibration split mostly changes the data the session goes on to collect. It halves over 150 votes without closing (still 12-17% of the cost level), and **42 is an ordinary sample** (percentile 0.52 +/- 0.02, n=75). The pre-registered reading holds: this is **not** an error bar to add to published numbers — the cell-to-cell sd a single-draw study sees (0.056) is what the decomposition predicts (0.057), so it is already inside every bootstrap here. What it is, is a floor pairing cannot lift, and a **user-facing lottery worth 0.03-0.05 in cost that has nothing to do with their labels** (#3806). Levels are this environment's; the portability handle failed (#3807). |
| [`2026-09-07-band-remeasure-3679/`](2026-09-07-band-remeasure-3679/REPORT.md) | Is #3156's band effect a LOWER bound (#3679)? Re-measures paired small-vs-large cost on the rebuilt 25-class construction and reports it beside the published run (`scale-3156-map`, job 582417). | **No -- it is an over-estimate.** The issue predicted the gap would widen; it **narrowed in all five arms**, by `0.033` to `0.065`, four of five resolvably. The reason is in the levels, not the gap: cost rose `6-14%` at `small` against `63-122%` at `large`, so part of the published effect was `large` being too easy -- the same shape as #3667's own 2.5x-at-`@small`, 1.25x-at-`@large`. Composition is **not** the confound (restricting both runs to the twelve shared classes agrees within `0.020` on every arm); power is, and the controlled comparison resolves 1 of 5 rather than 4 of 5. Five seeds by decision after per-cell cost came in at 19.1 min against the published run's 4.4 -- an estimate does not survive a pile rebuild. Measured on the pile as built 2026-09-07, before the 2026-09-08 rebuild. |
| [`2026-09-07-pool-hardness-3680/`](2026-09-07-pool-hardness-3680/REPORT.md) | Is the shared negative pool doing the same work for every class (#3680)? Per-class `AUC(positives, pool)` against `AUC(positives, cross-class)` on the rebuilt 25-class `vg_scale`, paired and with intervals. | The premise holds and the proposed mechanism is **refuted with the sign reversed**. The pool's difficulty is not uniform -- it spans `-0.066` to `+0.020` and the sign flips -- so one pool asks twenty-five questions. But #3680 predicted *indoor tabletop* classes would find the pool harder, and every one of them finds it **easier**, `book` (`-0.021`) included; the five that find it harder are `bird`, `knife`, `dog`, `boat`, `kite` -- outdoor and animal, though a query ablation shows *which* five is partly a property of the query: stripping the scene term from `a boat on the water` and `a car on the street` flips both across the axis, moving `car` by `0.041`. **Keep the single shared pool** (for 19 of 25 classes #3667's admission already sets difficulty, and the five exceptions carry the *smallest* margins), but stop calling it the hard-negative set and cite this as the number behind #3687's "cross-class ranking is not supported". The script also reported its own answer backwards -- "20 of 25" where its numbers said 5 -- fixed in `4bfb9798c`. |
| [`2026-09-06-vg-scale-promotion-3588/`](2026-09-06-vg-scale-promotion-3588/REPORT.md) | Promote #3588's thirteen reviewed classes into `SCALE_CLASSES` (#3588, #3604): twelve classes to **twenty-five**, the negative pool redrawn and the pile rebuilt. A cleared human review licenses only the class list; which VG spellings the class is *built* from is a separate measurement, and `SCALE_VG_NAMES_AUDITED` had never been run for the thirteen. 4,604 candidate names adjudicated against COCO at the cuts #3618 and #3636 shipped under. | **The audit was worth running, and adopting it verbatim deleted a class.** `cell phone` recovers **+1,280** images on the half COCO cannot score against the 535 it could already see (**+239%**), and `fire hydrant` goes from **44.7%** to **73.8%** of COCO's boxes on one spelling — how badly a plain class name misses is not predictable from the name. But the audit adjudicates one class at a time, so it offered `truck` as ambiguous evidence for `car`; `lift_ambiguous` pops the box before its exemptions are consulted, so that took `truck` from **3,386 band-free positives to 0 in all three bands** while the fold ledger still printed `truck+273/23` for a class that no longer existed. Six such names came out of it and the shipped invariant caught five — the sixth is a table *key*, which the pair test cannot see. Second defect, found by the same control: **`measure_supply.py` could not see the alias table**, so adding thirteen classes and 133 spellings changed its output by exactly **zero images** — and #3547 sized `SCALE_DEEP_N_POS` off it. 900 survives (`stop sign`, with no alias row, is still binding at 1,006), so that conclusion was right by accident. #3670's composition survives the expansion at **2.1×** headroom, and its realised prevalence is restated at **0.73%**. |
| [`2026-09-06-provable-negatives-3670/`](2026-09-06-provable-negatives-3670/REPORT.md) | Take `vg_scale`'s shared negative pool from 3,900 to 9,900 so a band cell sits at 1% prevalence instead of 2.5% (#3670), and decide what the extra negatives are made of: every one COCO-scored (`provable`), or matched to the positives' own 57% COCO share (`matched`). The pool is redrawn off the loader's own passes rather than rebuilt, so every number here is the number a build produces. | **The pool is `provable`, but not for the reason the trade was first written down with — and the headline number is 0.85%, not 1%.** A cell has scored the other eleven classes' COCO-scored positives since #3667, so the realised pool is ~1,900 larger than `SCALE_N_NEG`: designed **1.00%**, realised **0.85%** (0.844-0.856 over 36 cells, k\* **-6.87**). On composition, both candidates distort and on #3667's FPR scale they are **not separable** — the provenance shortcut an all-provable pool creates is **1.1x** (reverse arm, 1.06-1.12 over three embedders) against contamination's **1.18x** [1.09, 1.37] at #3666's *measured* 1.40%. An earlier reading quoted 1.32x and called it decisive; that took the top of a *predicted* range. What decides it is the **spread**: the shortcut is uniform across classes and contamination is not (1.04x-1.37x), and this dataset exists to compare classes and bands. The price is real — `provable` rules **513 of 743** reviewed negatives ineligible — but it loses **none** of the 147 it can hold, and telling those two apart is a change to the coverage gate itself. Two defects found on the way: `apply_corrections` folded verdicts for **any** class, so #3588 adding thirteen classes to the shared `corrections.json` made the shipped twelve-class build die with `KeyError: 'car'`; and `labels_exhaustive` was set by a **one-class review**, which would have admitted images to an "all-provable" pool whose absence claim is still VG's silence for eleven classes (now `coco_scored`, 197 images apart). The pile is **not rebuilt** — deferred so it is built once with #3588 — so `--verify` gains a pool-size and a composition check (#3678, #3681, #3690). |
| [`2026-09-06-cross-class-negatives-3667/`](2026-09-06-cross-class-negatives-3667/REPORT.md) | Rebuild `vg_scale` so a class is scored against images holding a *different* class (#3667), and measure what the rebuild actually bought. Eleven cells over three GRID jobs — `vg_scale`, `vg_scale_any` and `vg_scale_deep` × five embedders — then the built data checked against the price #3672 computed from the shipped pickle, and #3667's own claim tested with two probes. | **The change is real, it is worth +45.2% evaluable images per cell, and the contamination it removes lives almost entirely at one end of the axis this dataset exists to measure.** A head fitted the way the OLD benchmark posed the problem fires on the newly-admitted negatives **1.88 ± 0.19×** as often as on the pool it trained against (5.0% → **9.4%** FPR) — but split by band that is **2.50 ± 0.42** at `@small` against **1.25 ± 0.20** at `@large`, which is not resolvable from 1. So **#3156's published band effect is a lower bound by construction** (#3679). A text probe, which has no shortcut available to it, loses less than half as much AUC (**−0.012 ± 0.005** vs **−0.027 ± 0.007**) — the gap between the two probes *is* the shortcut. The price was good (+46.3% predicted, **+45.2%** measured); it read `categories` where `_evaluable` reads the labels, and **28.9%** of COCO-exhaustive positives hold a class they were never designated for. Three things the rebuild found that nothing else would: the merged fix was a **no-op on `vg_scale_deep`** and was writing 36 junk cell names per image into it (the #3156 guarantee, stated backwards, in a shipped pickle); the shipped cell was **ten days and five merged rulings stale** with nothing in the pile able to say so (#3678); and realised prevalence moved 7.14% → **~5.0%**, so the dataset's own `k*` is half a bit from the constant every study quotes (#3681). Vectors reproduce bit-for-bit on a repeat build and to 3e-07 across two GPU models, but **two of five embedders move by 3e-04 across a membership change** — batch composition, not the machine (#3683). |
| [`2026-09-06-shipped-pool-3666/`](2026-09-06-shipped-pool-3666/REPORT.md) | Has pool error for the twelve classes `vg_scale` actually ships ever been measured (#3666)? #3588 reported a `pool error` column of 0.0-7.1% for thirteen *candidate* classes and none for the shipped twelve, which would leave the benchmark with two tiers of label quality. Read out of the negative pass's 2,400 judgements; nothing rebuilt, `pile_config` untouched. | **It was measured — at 100 uniform draws per class against the thirteen's 70 — and the two tiers are not separable**: shipped **1.40%** [0.68, 2.86] against candidate **2.09%** [1.34, 3.24], a difference of **-0.69 +/- 1.39** pp. The seven classes asked inside a group needed an **attribution**, not another pass: **9 images**, because COCO settles every find on its own half for free, and none of the Vehicles or Table Objects finds is a `bus`, a `bicycle` or a `knife`. The real result is what the finds *are* — **6 of 9 are rulings that do not exist** for these classes (a wristwatch, a clock drawn on a monitor, a departure board, a pop-up canopy, the blank back of a sign), and ruling them out moves the union from **7.0%** to **3.0-5.0%**. So do not buy #3666's 840 draws: 380 more per class buy +/-1.0 pp where one sentence about wristwatches moved `clock` by **3.0** pp (#3673, #3674, #3675, #3676). Also: on the **45%** of the pool COCO scores, COCO reports a shipped class in **0 of 1,888** images and a candidate in **692** — the shipped 0.0% there is by construction, so a uniform draw wastes 45% of a reviewer's attention. |
| [`2026-09-05-pooled-names-3636/`](2026-09-05-pooled-names-3636/REPORT.md) | Can the VG names #3618 could not measure alone be adjudicated as a group (#3636)? 76 of 626 candidates fell below the five-image floor, carrying 312 non-COCO images that were neither acted on nor refuted. `name_evidence.py --pooled` scores a **group** with the same three cuts, counted over images, and lets only a name with no verdict of its own inherit; the grouping itself is declared in `pile_config` — `SCALE_VG_CONSTRUCTIONS` (colour, size, typing, count, spelling, plural) and `SCALE_VG_GROUPS` (hand-declared kinds, each with its criterion). `SCALE_CLASSES` untouched, nothing rebuilt. | **It works, and it is worth a third of what the issue estimated — because most families are not one hypothesis.** The gate that decides is **homogeneity**: over the members with a rate of their own, no member's Wilson interval may exclude the group's member-weighted rate (Bonferroni-adjusted). Constructions fail it **6 times in 69 (9%)**; hand-declared kinds **11 in 19 (58%)**. `bird`/`species` pools 24 names to 33% with **10 of 20 dissenting** — two populations, not noise: `geese` 1.00 and `dove` 1.00 against `chicken` **0.05** over 192 images and `crane` **0.02** over 123, which are the food and machine senses of a bird word. **25 names inherit** a verdict: **+54 repaired**, +18 withheld, **+27 net banded** on the non-COCO half, all of it in `umbrella` (+30), `dog` (+12), `bus` (+7) and `clock` (+5), and **every change an addition** — an individual measurement always wins, so nothing shipped was reversed. The sharpest result is negative: `boat`/`vessel` is sunk by **one member**, the word `vessel` at **0.23** against sixteen others between 0.69 and 1.00, and re-declaring the group after seeing that would be fitting the answer, so it is filed (**#3662**) rather than acted on. `stop sign` gains nothing pooled either — its ten `<what it says> sign` compounds pool to **7%** over 327 images — which is the last thing #3635's verdict could have been checked against. |
| [`2026-09-05-stop-sign-pool/`](2026-09-05-stop-sign-pool/REPORT.md) | Should `stop sign` be repaired, restricted or dropped (#3635)? #3618 found it the worst-served class in *C* — 18.7% self-match, and a 19,148-image `sign` family refuted at 7.9% — and offered a human pass, restriction to the COCO half, or removal from `SCALE_CLASSES`. This measures the thing all three assume. `pile_config` untouched, nothing rebuilt. | **None of the three, and the class is not the one the numbers indict.** Supply is not the problem: after `anchor_to_coco` the class holds **282 / 426 / 298** per band against `SCALE_N_POS` = 100, and the "496 VG images" is VG's *name* count on the half where the labels are COCO's anyway. What is real is **pool contamination**, measured here for the first time (`pool_contamination.py`, the unconditioned question a name-conditioned rate cannot reach): `stop sign` **1.17%**, *fourth* of twelve, against `backpack`'s **2.77%** — 112 expected hidden positives per 3,900-image pool, more than the 100 labelled ones per cell. `sign` still must not be listed, but not for the reason #3618 gave: the "12.7 images withheld" price defends itself on a pool that is **18.4x over-subscribed** (77,119 eligible, 4,200 drawn). A positive control refuted the obvious replacement argument — `bike` concentrates on the pool's hard negatives **more** sharply than `sign` (17.9x vs 6.8x at top-50), so *withholding hard negatives* is what every ambiguous name does. What discriminates is good hard negatives destroyed per contaminant retired, i.e. `1/precision - 1`: `bike` **31 : 30**, `sign` **435 : 37**. Recommendation: keep the class, extend the **shared** negative-pool review that already found 21 of them — one pass repairs all twelve (#3655, #3656, #3657). |
| [`2026-09-05-band-fold-3637/`](2026-09-05-band-fold-3637/REPORT.md) | Is a scattered fold the right outcome (#3637)? `canonicalise` merges an alias spelling's boxes into the class's and `band_for` bands the union, so folding a second spelling pushes 248 already-banded images past the scatter guard and out of every band (`clock` nets **−16**). Three readings — merge and let the guard judge, rescue the class's own band, or only ever add — scored against COCO's exhaustive boxes on the 51,411-image overlap, then run through the real build passes to designation. Nothing rebuilt. | **Keep it scattered, and the class really is scattered.** On the 225 images the modes disagree about, COCO says the class is not a single-band positive on **199 (88%)** — the fold's verdict is right **88%** of the time against **6.7%** for keeping the class's own band (**+81 ± 3.6** pp paired, McNemar χ² = 156), and it wins overall too (76% vs 75%, **+1.8 ± 0.14** pp). The structural argument is bigger than the ledger: `anchor_to_coco` **already** un-bands **17%** of the COCO half on evidence of exactly the same kind, so `guarded` would make the two halves of one dataset disagree about what a positive is. The cost is nil — **no cell** of 36 is under `SCALE_N_POS` under any mode (the scarcest has 144 for 100) and **no mode drops a reviewed image** (130 of the 3,600 designations carry a human verdict, so the hazard #3616 circled had something to hit and hit none of it). Two things fixed on the way: the fold now runs *after* the anchor (a verified no-op — identical ids in all 36 cells) and the old order over-reported the fold **2×**, 5,142 boxes against the 2,559 the build keeps. |
| [`2026-09-04-vg-name-coverage/`](2026-09-04-vg-name-coverage/REPORT.md) | What are the other eleven `vg_scale` classes' VG names worth (#3618)? #3617 fixed the mechanism by which a class built from one spelling turns its own instances into negatives, and declared `bicycle`; this measures all twelve. 626 candidate names, drawn from what lands on COCO's boxes **and** from each class's head-noun family, adjudicated against COCO over the 51,411-image overlap. `SCALE_CLASSES` untouched, nothing rebuilt. | **The measurement the issue specifies cannot make the decision, and the largest column in the study is the one you must not act on.** Fold-in is a box test conditioned the wrong way round: `sign` carries **46.6%** of COCO's `stop sign` boxes and a VG `sign` box is a stop sign **7.9%** of the time — 12.7 images withheld from the shared pool per contaminated negative removed (#3635). The test that decides is **repair precision** — COCO adjudicating the images where a name is the class's only evidence — and `1 / precision` reads directly as that price. 32 names fold, 50 are withheld, 468 refuted: **860 images repaired** and 2,664 withheld on the non-COCO half, `bird` +18%. Three method results: the box-overlap test **cannot see a spelling split** (`backpack`/`backpacks` scores 0.000 both ways, 846 of 1,740 pairs never co-occur); VG's synonym list is a list of **one**, always, so there is no free fix in the reader; and the head-noun shortcut is a trap — the largest member of `dog`'s family is **`hot dog`**, 0 of 181. A fold also un-bands 248 images via the scatter guard, and `clock` nets **−16** (#3637). |
| [`2026-09-03-vg-scale-classes/`](2026-09-03-vg-scale-classes/REPORT.md) | Which classes can `vg_scale` actually carry, and what must their names say (#3588)? A measurement pass over VG supply, measured name aliasing and definition risk for every class the issue proposes, plus the review material for what survives: 13 slates, 13 datasets, 13 empty detectors and an [annotation guide](2026-09-03-vg-scale-classes/ANNOTATION_GUIDE.md). `SCALE_CLASSES` is untouched and `vg_scale` is not rebuilt. | **The issue's own proposal does not survive the gate the issue specifies**, and the reason is structural: every scene-exclusive class it wanted (`train`, `zebra`, `giraffe`, `elephant`) fails on the **small** band — a class that owns its scene is photographed filling the frame — so context exclusivity and small-band supply are anti-correlated in VG, and the easy end of the axis cannot be widened from this source (**#3603**). Thirteen classes survive, none of them from the headline tiers. Definition risk is measurable **before** anyone labels: `coco_folds.py` prints `magazine` (79) and `magazines` (30) under `book` — the split that cost that pass 49 verdicts — and scores `book` at 43%, which calibrates `cell phone` (46%) and `cup`'s 1,136 `glass` boxes. The rule then travels in the **dataset name**, the only string a reviewer sees while voting. The thirteen would also evict 34% of the shared negative pool against 300 spares (**#3604**). |
| [`2026-09-03-drive-cold-3521/`](2026-09-03-drive-cold-3521/REPORT.md) | Does the corrected `--drive` produce a *usable* timing profile (#3521)? Two legs of one workload on one node, differing only in whether the driver arranges the caches a step's cost forks on, each leg the other's held-out set. Scored on **bar error** — the fraction of the progress bar budgeted to the wrong step — reported **per branch**. | **The issue is real, its remedy was aimed at the wrong mechanism, and one of its four instances is misdiagnosed.** A profile fitted as dev's driver produces one puts **0.94** of a dataset-open's bar in the wrong step when the atlas rebuilds — six times worse than shipping **no profile** (0.15) — and the fix trades it for 0.34 there at the cost of 0.49 on the restore, where the old one was perfect. No cell can pace both branches (**#3594**). On the *embed* fork, where the branch can simply be arranged, the win is unambiguous: **0.35 → 0.00**, and the default `--reps 2` was fitting a slope through one real embed and one zero. Two corrections to the tree: `dataset_stage`·`embed` never measured embedding on **any** branch — the demo importer embeds inside `acquire` (**#3593**) — and the "minutes-long" atlas rebuild is **under 9 s** at every size swept, reaching minutes only near the 50k threshold (**#3595**). |
| [`2026-09-02-timing-r2-3345/`](2026-09-02-timing-r2-3345/REPORT.md) | What does the timing profile's r² actually say, now that #3334 keeps one (#3345)? Three profiles read apart: the generic recorder across five task families, the older per-load profiler on the **same** eight imports, and the #3062 resweep's 4,771 rows refit through this fitter. Every fitted step also scored on the prediction error a progress bar would feel. | **`embed` and `finalize` are cleanly affine (r² 1.00 / 0.99, 3 % / 7 % error); every step the shipped pacing thinks *dominant* carries no r² at all, for one reason.** `--drive` measures the **warm** occurrence of each once-per-process cost, so the fitted profile gives the model load **0.00** of a 412-image import where the shipped cost model gives it **0.79**, and 2 % to a coverage atlas the defaults weight at 85 %. A high r² is not a well-paced step (`serialize`: r² 0.99, error 69 %) and a low one is not always a bad step (`coverage`: r² 0.03, error 17 %). Rollup cells — the ones that always match — run r² 0.29 / 50 % error against 1.00 / 3 % exact. Four defects fixed: nothing read the r², both recorders stamped a blank embedder, and `dataset_promote` was silently unmeasurable against a removed endpoint. |
| [`2026-08-31-enrich-descriptions-3127/`](2026-08-31-enrich-descriptions-3127/REPORT.md) | Should `enrich_descriptions` still default to `False`, now that #3077 made `clap_general` the audio default (#3127)? Enrichment on/off for the default embedder of **every** media type - 22 eval datasets, 560 paired (dataset, category) queries - plus `clap` and `bge` controls. Executes [`PLAN.md`](2026-08-31-enrich-descriptions-3127/PLAN.md), pre-registered before the run. | **Keep `False`; the setting is one boolean doing four jobs.** It is worth **+0.014 +/- 0.009** on the audio default (below its own 2 SE, and it is one template - `the sound of {text}` - not the ensemble), inert on `siglip`, and **-0.057 +/- 0.009** on the text default, worse on 45 of 45 categories and reproduced on `bge`. #3077's sign flip reproduces exactly. The E5 query/passage asymmetry, the obvious mechanism, accounts for only a quarter of the damage. Follow-up: make enrichment per-embedder. |
| [`2026-08-30-fit-quality-3329/`](2026-08-30-fit-quality-3329/PREREG.md) | Is the 2-component Gaussian mixture a **good** fit, not just the best one (#3329)? Absolute goodness-of-fit for the mixture that sets every threshold the app computes: tail calibration at the cut, class-conditional shape on the logit axis, component-to-class identification, and whether the shipped k=0.3 anchoring moves the fit at all. 192 cells on `vg_scale_any`, three geometries. | **Pre-registered, not yet run** - the plan is linked because no report exists yet. Every fit diagnostic in the tree is *relative* (one family against another, one cut against another), so a misspecification both families share is invisible to all of them; this is the first absolute measurement. H4 is pre-registered so that "the fit is wrong and it costs nothing" is a reportable finding rather than a null. |
| [`2026-08-29-inclusion-knob-3196/`](2026-08-29-inclusion-knob-3196/REPORT.md) | Does the Inclusion knob still have authority under the shipped linear SVM head (#3196)? 21 stops of the slider x 5 cut rules, two full head arms, 576 cells on `vg_scale`. | **The premise does not reproduce on real data, and the sign is the other way round** - the knob is at least as live under the SVM and significantly livelier in both DINOv3 environments. Nothing ships. The flat band the issue found is real but its axis is the **target**: dead-step rate rises monotonically small -> large under both heads, and 7 of the 8 individually dead region cells are `@large` (`clock@large` in all four seeds). |
| [`2026-08-28-voted-exclusion-3308/`](2026-08-28-voted-exclusion-3308/PLAN.md) | Did the #3308 voted-media exclusion buy anything on real data, and is `EXCLUSION_MIN_REMAINDER = 60` the right floor (#3312)? Two stages: production scale, where the floor is inert, and deep voting on a modest collection, where the remainder crosses every arm's floor. | **Pre-registered, not yet run** — the plan is linked because no report exists yet. Both numbers behind the shipped floor today are synthetic; the live hypothesis is that the change is a rigor improvement with no measurable benefit at production scale, and the design makes that a reportable finding rather than a null. |
| [`2026-08-28-calibration-fold-count-3310/`](2026-08-28-calibration-fold-count-3310/REPORT.md) | Does more cross-calibration ever pay, once its wall-clock price is charged (#3314)? Executes [`PLAN.md`](2026-08-28-calibration-fold-count-3310/PLAN.md), pre-registered before the run. | **Keep `calibrate_count = 2`** — the gate closes on **cost, not benefit**. More folds do help, early and only on the DINOv3 geometries (−0.0057 ± 0.0012 at K=6, 1–25 votes, region voting), but every fold count that clears the margin costs ≥ 2.3× the user's per-step retrain against a 1.5× ceiling; K=3 is affordable and half the margin. 86% of a fold's price is the anchored EM — make that cheaper and K=6 lands inside the ceiling. |
| [`2026-08-27-good-mining-3267/`](2026-08-27-good-mining-3267/REPORT.md) | Does a different Autopilot **opening** mine better Goods (#3267)? Eight openings x 24 environments x 42 seeds, 200 clicks each, every cell seeded from a typed query. | **`top_long` (`g8@top,b4@mid`) — just take more off the top**: +5.8 positives/100 clicks and −0.018 cost against `prod`, and +11 against the length-matched control, so it wins on *where* it clicks. Yield falls monotonically with sampling depth and the gain is concentrated in **scarce** categories (+9.5 vs +2.5). The shippable Inclusion lever gets ~65% of it, but only because `k-10` **saturates to `@top`** — it is a lossy way of asking for the top of the sort. |
| [`2026-08-27-calibration-fraction-3287/`](2026-08-27-calibration-fraction-3287/REPORT.md) | What Train/Calibrate split should a detector use (#3287)? `calibration_fraction` has been `0.5` since it was introduced — the obvious default, never measured — and it sits on the shipped threshold path. | **0.5 is not optimal on the shipped default**, and the axis is the **embedder, not the voting mode**: `siglip` wants 0.4 (−0.013 ± 0.003, winning in every vote band), `siglip2_l` and both CLIP capacities want 0.3, and `dinov3_patch` wants 0.5 in *either* voting mode. So the per-mode default the issue proposed would average across a disagreement. Evidence only; no production change proposed. |
| [`2026-08-25-calibration-fold-combine/`](2026-08-25-calibration-fold-combine/REPORT.md) | Pooling fold scores or averaging per-fold cuts (#3115) — two docstrings in the repo assert contradictory premises about whether fold scores are comparable. | **Both are wrong, in different places.** Averaging beats pooling in both voting modes; the comparability premise is **mode-dependent** (quantile space is worth −0.032 on region and costs +0.036 on binary). The contamination argument is **prevented by the stratified splitter** and its cited evidence conflates a degenerate *cut* with a degenerate *holdout*. Production's anchored rule is untouched. |
| [`2026-08-25-vg-scale/`](2026-08-25-vg-scale/REPORT.md) | Where does VTSearch stand on same-class target size (#3156 / #3276)? 12 classes x {small, medium, large} x 5 embedder columns x 20 seeds, shipped defaults, 3600 cells. | A map, not a decision: cost is ~4x higher on sub-patch targets than on large ones and four fifths of it is the RANKING, not the cut; region voting is lowest in every band without removing the size effect; and the column with the best free text sort (`siglip2_l`) takes a median 29 clicks to beat it. Carries an interactive [`viewer.html`](2026-08-25-vg-scale/viewer.html) over every slice. |
| [`2026-08-24-transfer-2883/`](2026-08-24-transfer-2883/REPORT.md) | Is the #2836 decomposition's dominant `transfer` term a real cost (#2883)? 552 cells, [pre-registered](2026-08-24-transfer-2883/PREREG.md) before submission. | **`transfer` is not a cost — it is a variance measured against an optimistic reference.** All four pre-registered hypotheses land on both arms independently; one sub-prediction inside H4 was wrong and is called out. |
| [`2026-08-21-inclusion-cut-rule/`](2026-08-21-inclusion-cut-rule/REPORT.md) | Which cut rule should answer the Inclusion knob (#2865)? Five candidate rules × thirteen stops of the knob, scored at their own inclusion, on the two region-voting environments plus their binary controls. | Keep the shipped `mid_tilt`: it delivers 95% of the slider and nothing beat it without losing materially somewhere. The `mid` cut it replaced is **inert** — one admitted set for the whole knob in every measured step. |
| [`2026-08-19-linhead-convergence-2808/`](2026-08-19-linhead-convergence-2808/REPORT.md) | Is the linear head's spike reduction limited by early stopping (#2808)? 450 cells over two datasets × two embedders. | **No — and the question was built on a misread number** (#2847's conformal-threshold pair, not the threshold we ship). Convergence buys −0.016 ± 0.005 in worst-step regret, vanishes on the default embedder and costs ~5.3× training: **do not raise `TRAIN_EPOCHS`.** What is worth acting on is +1.2 ± 0.5 positives — positives, not spikes, are the binding constraint. |
| [`2026-08-18-fastproc-3146/`](2026-08-18-fastproc-3146/REPORT.md) | Should the image embedders switch to the fast (torchvision) processor (#3146)? | **They already had.** `transformers` 5 removed the `Fast` suffix, so the flip happened silently inside our own `>=4.49` pin. GPU preprocessing — the issue's other proposed fix — is **not** cleared: a 1013-cell power run leaves `cost` and `fnr` grazing the 0.005 margin for 1.09× per cell. What outlives the issue is a **second unrecorded axis on the pile** — which backend built a cell. Narrative version: [`report.html`](2026-08-18-fastproc-3146/report.html). |
| [`2026-08-18-gpu-node-3160/`](2026-08-18-gpu-node-3160/REPORT.md) | Why do two nodes both answering to `gres/gpu:v100` produce `siglip2_l` vectors 1.5e-04 apart (#3160), 30× what the calibration studies resolve? | **A type is not a device — and the device was never the problem.** Two V100 parts fed the same input compute bit-identically at every one of 27 blocks; the divergence enters on the **host**, where the 384 px resize rounds differently under AVX-512 than under AVX2. The nine outlier nodes are exactly the nine whose CPUs have no AVX-512. |
| [`2026-08-17-embed-precision-3143/`](2026-08-17-embed-precision-3143/REPORT.md) | Can the image encoders run in half precision (#3143)? Ten arms, precision crossed deliberately with the card. | fp16 is a **very small perturbation** — 2.9e-6 cosine on `siglip2_l`, retrieval order intact, no detectable benchmark effect at 1013 paired cells — so adopt it selectively for the heavy encoders, but **do not flip the default**: `siglip` gains nothing and a global flip would strand the fp32 pile. `bf16` is disqualified. §5 is where the cross-node 1.5e-04 was found, chased in [`2026-08-18-gpu-node-3160/`](2026-08-18-gpu-node-3160/REPORT.md). |
| [`2026-08-12-overview-bench/`](2026-08-12-overview-bench/REPORT.md) | What does a user actually get from each shipped configuration (#3129)? 3 representations × 6 haystacks × typed-vs-clicked, at production defaults. | A characterization, not a ship decision: boxes are worth more than the encoder (strip them and DINOv3 is worse than the shipped default), target scale is the strongest axis, positives are the binding constraint, and VG's own labels bound how good any of these numbers can look. |
| [`2026-08-12-calibration-fold-count/`](2026-08-12-calibration-fold-count/REPORT.md) | Is 2 still the right number of cross-calibration folds (#2897)? A counterfactual screen plus a live A/B, decision rules fixed as module constants before the run. | **Keep `calibrate_count = 2`; the study ships nothing.** Binary voting says so outright — no K beats 2 by the margin and more folds monotonically hurt in the deep regime. Region voting mechanically recommends K=6, but at 2.68× the interactive retrain, with its live effect below the ship margin once acquisition feedback moves, and the mechanism check that was meant to validate it **cannot be read** (a defect in the instrument, not a result). Follow-ups #3115, #3116. |
| [`2026-08-07-acquisition-inclusion/`](2026-08-07-acquisition-inclusion/REPORT.md) | Should the acquisition threshold be decoupled from the reporting threshold? (follow-up to #2847 / PR #2873) | Shipped; the design now lives in [`docs/ML.md`](../ML.md#threshold-calibration). |
| [`2026-08-07-spike-check-2847/`](2026-08-07-spike-check-2847/REPORT.md) | Do the MLP-era cost spikes survive today's stack (#2847)? | Better — and it is mostly the *threshold*, not the head. |
| [`2026-08-05-population-anchored-calibration/`](2026-08-05-population-anchored-calibration/REPORT.md) | Fuse the haystack distribution into the trained threshold instead of scheduling it out (#2852 / #2853). Two runs: deep-regime, then anchor-mass sweep. | Fold-anchored mixture at κ=0.3 with the `mid` cut; fusion does **not** beat the blend on binary voting — a live regression tracked in [`population-anchored-calibration.md`](../plans/population-anchored-calibration.md). |
| [`2026-09-13-docmarks-v3/`](2026-09-13-docmarks-v3/REPORT.md) | Is DocMarks ground truth yet (#3343 stages 3-5)? Pick the roster, adjudicate every instance and every pair, build the cells. Narrative: [`report.html`](2026-09-13-docmarks-v3/report.html). | Yes for 24 classes -- 613 instances examined and **0 rejected**, **all 276 pairs** adjudicated rather than sampled -- but the finding is that none of it would have survived being rebuilt, and stage 3 *is* a rebuild. Four defects, each turning a hand decision into silent corruption: adjudications keyed on a **page id** rather than a mark, so the one merge the previous audit recorded would have fused a 27-instance logo class, a 29-instance logo class and a stamp class into one (#3835); `split` and `membership` verdicts **never written** to the only file a rebuild replays (#3836); a hand-merged class **renamed** by the next build (`spods/stamp_00551_1` came back `spods/stamp_00546_1`), drifting any roster naming it; and `embed_corpus.py` **unable to write a cell at all** -- it surfaces at the last line, so tier `s` embedded all 5,000 pages over 2h16m and then died on `ModuleNotFoundError`. The control now reproduces the audited partition exactly: 58 classes, byte-identical membership, 0 changed. The roster is picked *against* the shortlist ranking, whose top 24 are 21 SPODS logos on a pool that is already 71% SPODS. |
| [`2026-09-02-docmarks-v2/`](2026-09-02-docmarks-v2/REPORT.md) | The full-scale DocMarks build (#3343): all four sources, 200,000 pages, three nested tiers. At 130x v1's size, is the corpus one an eval can be trusted on? Narrative: [`report.html`](2026-09-02-docmarks-v2/report.html). | Yes for SPODS and Tobacco800, no for UCSF. Four defects a green build had hidden: StaVer contributed **zero pages** (mask stems carry a `-px` suffix the scans do not); Tobacco800 contributed **zero classes** (hardcoded out of clustering, because its signatures ship identities and its 432 logos do not); the Tobacco800 contamination guard was **inert** (`industry` is indexed but not stored in UCSF's Solr, so every page recorded `null` and the same-archive exclusion never fired); and the 200k budget was **unreachable** under an even six-way industry split, three of which are nearly empty -- Fossil Fuel has 311 documents in total. One threshold also does not fit four sources: swept separately, StaVer wants 0.04, SPODS 0.10 and Tobacco800 0.18, where it yields 5x the classes. UCSF letterhead bands are **not clustered at any threshold** -- the band is a fixed top-of-page crop, so the descriptor measures page layout, and the largest component is already 12.4% at the grid's lowest value; at 0.10 it made one 12,706-instance "class". |
| [`2026-08-31-docmarks-v1/`](2026-08-31-docmarks-v1/REPORT.md) | Does fixing the mask decomposition fix the DocMarks class inventory (#3361)? Same 1,088 SPODS pages, same generator, marks drawn correctly. Narrative: [`report.html`](2026-08-31-docmarks-v1/report.html). | Yes — median mark 276→376 px, the 46% "largest mark" and all 1,196 `text` pseudo-marks gone, and v0's bimodal size spike exposed as the fragments it always was (84% of marks now in one 256–512 px bucket). It also moved a number nobody expected: the clustering threshold is a property of the marks, so v0's 0.16 now chains 31.8% of the corpus into one class while still reporting 310. Re-swept to 0.10. |
| [`2026-08-31-docmarks-v0/`](2026-08-31-docmarks-v0/REPORT.md) | What does the DocMarks stamp-detection corpus actually look like? A look-at-it pass over 1,088 SPODS pages and 432 UCSF distractors — no scoring. Narrative: [`report.html`](2026-08-31-docmarks-v0/report.html). | The data is right and the labels are not yet. Marks sit entirely above the 32 px floor at 0.76% of a page (bimodal, so band rather than average). Looking caught three silent defects counts could not: inverted masks reading the paper as one page-sized mark, a fixture-tuned threshold putting 87% of marks in one component while reporting 34 classes, and a 64-bit hash unable to separate a book stamp from an elephant stamp. |
| [`2026-08-04-gmm-cut/`](2026-08-04-gmm-cut/REPORT.md) | Where should the GMM path place its cut (#2836)? Autopilot simulation plus a synthetic bench with known ground truth. | See BLUF; the #2846 remeasurement and the #2881 tail-α run ([pre-registration](2026-08-04-gmm-cut/PREREG-2881.md), [result](2026-08-04-gmm-cut/REPORT-2881.md)) close the EVT cut line — beating production now needs a better *fit*, not a better cut. |
| [`2026-08-04-mixin-schedule/`](2026-08-04-mixin-schedule/REPORT.md) | How safe should safe-thresholds be (#2841) — how much GMM, for how long? | Neither voting mode should ever hand over: `slow_cap50` for region, `cap50` for binary. |
| [`2026-08-03-safe-thresholds/`](2026-08-03-safe-thresholds/REPORT.md) | Should `safe_thresholds` be forced on for everyone (#2799)? | Yes — and the setting was later deleted rather than left as a way to opt into a worse threshold. |
| [`2026-07-31-calibration/`](2026-07-31-calibration/REPORT.md) | Trained vs oracle thresholds for Autopilot (#2781) — run twice, before and after PR #2784. | The runaway threshold was the conformal walk's `k=0` anchor, not the `NO_GOOD_THRESHOLD` sentinel; the raw-patch tree is calibration-bottlenecked and no pre-registered remedy recovers it. |
| [`2026-07-29-max-patch/`](2026-07-29-max-patch/REPORT.md) | Does the HAC region tree earn its keep against raw-patch max-pooling? | Ship tree-free MaxPatch; drop the HAC tree from ingest (#2886). |
| [`2026-07-27-inclusion-knob/`](2026-07-27-inclusion-knob/REPORT.md) | Why doesn't the Inclusion slider move the included set (#2693), and what mechanism would? | Plus [`SELECTION-BIAS.md`](2026-07-27-inclusion-knob/SELECTION-BIAS.md), the study of what the detector's own sort costs the calibration set. |
| [`2026-07-22-mlp-vs-svm/`](2026-07-22-mlp-vs-svm/REPORT.md) | Would a linear or RBF SVM rank better than the detector MLP? | Keep the MLP — no variant met the pre-registered switch criterion. **Superseded as a product decision:** it measured the auto-sized MLP head, and the shipped head has since moved to a linear SVM; see [`docs/ML.md`](../ML.md#the-three-heads-which-one-is-shipped-and-why). Narrative version: [`report.html`](2026-07-22-mlp-vs-svm/report.html). |
| [`2026-07-22-vtsbrowse-umap-tuning/`](2026-07-22-vtsbrowse-umap-tuning/REPORT.md) | How should UMAP's dials be set for the Browse projection? ~5,000 scored fits over 23 embed sets. Narrative-only: [`report.html`](2026-07-22-vtsbrowse-umap-tuning/report.html) is the record. | `n_neighbors` tracks the embedder (10 image / 15 audio), `min_dist` barely matters, and compaction consistently costs layout quality — so it **ships off**. |
| [`2026-07-13-screenshot-iconography/`](2026-07-13-screenshot-iconography/REPORT.md) | Does structural search work on flat rasters — screenshots and scans? The OpenLogo follow-up. Narrative-only: [`report.html`](2026-07-13-screenshot-iconography/report.html) is the record. | **SIFT is the bottleneck on line art** (5.1% vs SuperPoint+LightGlue's 41% true-pair verify); SP+LG as ranker beats SigLIP on both document corpora — the first time structural search wins on a real corpus. 3-DoF is a free precision win; LightGlue needs its own inlier floor (~24); 224 px tiling rescues small targets. |
| [`2026-07-12-toponymy-image-signposts/`](2026-07-12-toponymy-image-signposts/REPORT.md) | Can Toponymy name the neighbourhoods of an *image* browse map? 8 image→text strategies across photos, screenshots and scans, including re-fitting signs over a Find result. Narrative-only: [`report.html`](2026-07-12-toponymy-image-signposts/report.html) is the record. | Shipped the winning strategy into `vtscore/projection/signpost_texts.py`. |
| [`2026-07-12-toponymy-audio-signposts/`](2026-07-12-toponymy-audio-signposts/REPORT.md) | The audio half: what is the right audio→text strategy to feed Toponymy? 4,445 clips, 4 strategies, 2 namers. Narrative-only: [`report.html`](2026-07-12-toponymy-audio-signposts/report.html) is the record. | Shipped the winning strategy into `vtscore/projection/signpost_texts.py`. |
| [`2026-07-11-structural-search-openlogo/`](2026-07-11-structural-search-openlogo/REPORT.md) | Seeded with one real-world logo crop over 27k unstaged photos: how far does SIFT+VLAD get, does 6-DoF beat 4-DoF, and what do labels buy? Narrative-only: [`report.html`](2026-07-11-structural-search-openlogo/report.html) is the record. | **Stage-1 VLAD recall is the ceiling** — 2.5% of true instances reach the top-50, against SigLIP cosine's ~10× stronger showing. 4-DoF wins; on the structural path labels are calibration, not learning. Also caught the deterministic **30th-vote transient** in the shared MLP trainer. |
