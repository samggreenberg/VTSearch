<!-- _class: caveat -->

## What we do not yet know

- Region voting is only tested where **boxes exist** — most corpora have none
- **~65%** of the remaining gap is sim→test transfer, which no cut rule fixes
- Starved runs fail *silently*: no positives, no error, no row

<!-- Say this before someone else does. The silent-failure point is the one
     that matters operationally. -->
