<!-- _class: full -->

![bg fit](figs/atlas-blindspot.png)

## Off the Map

<!-- build: figs/atlas-blindspot.build1.png -->

<!-- build: figs/atlas-blindspot.build2.png -->

<!-- build: figs/atlas-blindspot.build3.png -->

<!-- This is the one thing the loop cannot do for itself, and it is worth being
     precise about why. Asking at the line is not a lazy heuristic — it is the
     right question when the model's mistakes are near the line. The claim here
     is narrower: not every mistake is. -->

<!-- **a** — The plane again, exactly where Pics on a Plane left it: a detector,
     a handful of votes, and eighty-odd items nobody has looked at. -->

<!-- **b** — Every question the loop has asked came out of this strip. That is
     by construction — autopilot picks the unlabelled item nearest the cut — and
     it is efficient, because an answer near the line moves the line. -->

<!-- **c** — Now a region of the same collection with no vote anywhere near it.
     Nothing in the loop will ever produce a question there, and the reason is
     the trap: the model is not **uncertain** about that region. It is a long
     way from the line, so it is confident. -->

<!-- **d** — And it is full of books. Confidently wrong, and silent about it —
     the one failure a boundary probe cannot surface, because the probe goes
     looking for *doubt* and there isn't any. A concept is rarely one blob in
     embedding space: "book" has a shelf mode and a held-in-hands mode, and the
     twenty-minute budget buys a tube around whichever one the first vote
     landed in. -->

<!-- So the question the atlas answers is not "what am I least sure about?" but
     "where have I not been?" — and those are different questions with different
     answers, which is why the autopilot asks both. -->
