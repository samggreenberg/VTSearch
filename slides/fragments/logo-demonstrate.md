<!-- _class: statement -->

# “Logo detection” is not one task.<br>It is four tasks sharing a name,<br>and they disagree about the answer.

<!-- Land the appendix. None of this is about model capability: a perfect
     detector still has to be told which of the four it is, and nothing in the
     phrase tells it. -->

<!-- The classical route makes it worse rather than better. A pretrained "logo"
     class ships somebody else's boundary, drawn for somebody else's job, and
     you find out which one at the far end of a labelling budget. -->

<!-- Our answer is the same one the `book` slide gave: do not ask for the
     boundary in writing, ask for it in votes. Twenty minutes of Good and Bad
     puts it where the person actually meant, and autopilot spends its
     questions on precisely the disputed band — the unreadable disc, the
     parody, the Diet Coke. -->

<!-- And the two halves of this talk are the two halves of the problem. The
     votes say **which concept**; Inclusion says **at what price** — a
     sponsorship count would rather over-count than miss, and brand compliance
     is the other way round. -->

<!-- If someone asks why a careful written spec would not do: it would, and
     then you would have to check it — which means looking at items and saying
     yes or no. That is voting, with extra steps and no calibration. -->
