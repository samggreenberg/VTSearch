### Next

## Attack the Fit, Not the Cut

- **Characterize transfer** — what does the calibration set fail to say about the pool?
- **Schedule the anchor mass** — the best κ falls with votes; κ ∝ 1/*n* is a one-line arm
- **Un-strand binary voting** — fusion only ties the blend there; gate it or split it

<!-- Deliberately three items and no more. The headline states the strategy
     the epilogue earned: the cut axis is closed, so the open work attacks the
     fit.

     Take them in order. Transfer characterization is the designated successor
     line — the error decomposition says that is where the remaining regret
     lives, and nobody has yet characterized what the calibration set fails to
     say about the pool it is used on. Scheduling the anchor mass follows
     straight from the caution two slides ago: the best κ falls as votes
     accumulate, so a schedule that decays it replaces a compromise constant
     with the trend the data already showed, and it is a one-line arm. And
     binary voting is on record as stranded: fusion ties the capped blend on
     both binary environments, but the off-switch is gone, so binary users are
     running a threshold that never beat their best alternative. Either gate
     fusion by voting mode, or split the setting.

     If someone asks "why not just try a better cut rule" after all this: that
     is exactly the point of the epilogue — the axis has been measured to be
     empty, which is why none of these three is a cut rule. -->
