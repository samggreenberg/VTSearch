<!-- _class: full -->

![bg fit](figs/logo-hits.png)

## The Real Thing

<!-- build: figs/logo-hits.build1.png -->

<!-- build: figs/logo-hits.build2.png -->

<!-- **a** — One query, typed the way anyone would type it. Eight results, and
     this is not a curated set of hard cases — it is the first page. Read them
     down the left. Every one of them is a Coca-Cola logo. Every one of them is
     a different picture. -->

<!-- **b** — What each one actually has. The wordmark reversed out of a solid
     red field; the round button badge; that badge with a contour bottle beside
     it; the wordmark over the dynamic ribbon; "Coke" set in a heavy sans that
     is not the script at all; the wordmark in flat black; the wordmark in red
     on white; and Diet Coke, which is a different product. -->

<!-- **c** — Now read down the columns instead. **Script** is in seven of the
     eight — the heavy sans is not it. **Red** is in seven of the eight — the
     black one has none. Those are the two best columns, and the two anybody
     would name if asked what a Coke logo *is*; every other column is in low
     single figures. No attribute is in all eight. -->

<!-- So there is no feature a detector could key on that returns this set — and
     this set is what the person who typed the query got back. That failure is
     not the model's. The words never named a set. -->

<!-- If it helps to land it: this is the `book` slide again, in the one domain
     where the room expects it *not* to bite. Everybody already accepts that
     "book" has a fuzzy edge. A logo is a designed artefact with a published
     spec sheet, and it is **worse** — because the disagreement here is not at
     the edge of one concept, which is the next slide. -->
